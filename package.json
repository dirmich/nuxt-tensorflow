{
  "name": "nuxt-tensorflow",
  "version": "1.0.0",
  "description": "Tensorflow example for nuxtjs",
  "author": "David, Shin",
  "homepage": "https://dirmich.github.io/nuxt-tensorflow/",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "predeploy": "cross-env USE_SUBFOLDER=true nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --verbose"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^1.7.2",
    "ant-design-vue": "^1.1.10",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
